# Payload burst continuation — Goal remains active

2026-09-07. The previous turn made verified progress (continuous-activity commit90cc3c0); this continuation adds the remaining idle-separated cumulative payload behavior. Existing Explore Summary stays linked to the current main Trend. No installation or full-goal completion is claimed.

- New Explore preset22, Cumulative data within bursts: device-wide activity runs merge across Idle <=0.5ms; a greater gap resets cumulative completion-counted R/W payload. First request included; Discard/Flush/Other extents excluded. Shared activity boundaries precede analysis filters. Unknown coverage is unavailable.
- Main step curves show the within-burst Total/Read/Write accumulation. Center markers show final burst totals and define rectangle selection; clicking selects a whole burst. Summary uses one final payload total per burst, including zero filtered payload. All cumulative timestamps/bytes, final totals and bounds are exported. Window/interval selection no longer needlessly clones the entire series before keeping the selected samples.
- Deterministic regressions cover exact/above-threshold gaps, first request, unordered completion input, non-R/W extent exclusion, another-process filter, zero filtered burst payload, unknown coverage, exact selection and cumulative CSV.
- Native real Samsung device8:0 trace:323 bursts/8247 requests/185200640 R/W bytes. Final-total P50=.0625MiB/P95=2.1484375MiB. Read filter:323 unchanged bursts/7415 requests/169197568 bytes. Point:1 burst/2 requests/65536 bytes. Area:170 bursts/540 requests/23097344 bytes. Independent original-event oracle matches every boundary, cumulative point, final total, percentile, histogram, detailed CSV and both BW results. Legacy184-request unproven coverage shows no invented burst distribution.
- Actual uPAS five assignment nodes executed on equal three-request fixture. It zeroes the first payload after Idle exceeds0.5ms, yielding[1,3,0]MiB; corrected first-request-inclusive output[1,3,4]MiB. Source differences and clock/filter semantics are documented in UPAS_ANALYSIS_CONTRACT.md.
- Native full-burst screenshot reviewed. Compatible all-target desktop/protocol/types tests pass (desktop lib79 passed/2 environment-dependent ignored); compatible Clippy -D warnings, fmt and diff checks pass. Linux/Android build acceptance is separate. Full before/after performance and memory measurements remain required; native debug timings are not a performance completion claim.

Evidence: outputs/graph-summary-validation/burst-oracle.json, burst-upas-comparison.json, burst-all-tests.log, burst-clippy.log, burst-*.json/png/summary.csv/io.csv. Reproduction helpers: work/verify_bursts.py, work/compare_bursts_upas.py (python -I), work/checkpoint_bursts.py.

Next required work: Timeline/Gantt/connected Footprint; capture-field feasibility and implementation audit (including completion CPU, hardware queue/IOWait where feasible); all43 corresponding-input/real/synthetic/missing/large graph validation; all Apply/Clear/filter edge semantics; before/after performance/memory; compatible agent/object builds; original source and installed manifest recheck; backed-up Release installation and actual installed launch. Keep the full Goal active and continue available work. Current phone lacking su only limits physical eBPF acceptance; previous actual Perfetto capture/save/reload remains separately verified.

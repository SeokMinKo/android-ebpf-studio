# Timeline / Gantt checkpoint — Goal remains active

Implemented Explore block command Timeline (preset24) and request Gantt (preset25), with the existing right Summary showing the same cohort's device latency distribution and request categories. Both use full-resolution background summary data. Separate devices remain distinct, absent issue has completion only, and Gantt has40 rows/page with full-cohort statistics independent of page. Existing Compare presets remain; newly specialized window/lane/timeline presets are excluded from its scatter-only picker.

Native verification used the existing Samsung8294-I/O Perfetto session, device8:0=8247, with Read7415. Point selects1; Timeline half-window area selects556 and23,097,344 payload bytes; first-page Gantt area selects38 and696,320 bytes. Gantt page100 selection/Zoom/Back passed; last page and all207 Next-page actions plus Previous were exercised. All8247 row labels were visited once before return, and every page retained8247 Summary samples and the same P50/P95. Synthetic segment-midpoint click selects the intended400ms/4096B request; missing-timing fixture retains3 requests/12,288B with2 measured latency samples and1 missing.

`work/verify_timeline.py` independently pairs raw events, compares full issue/completion coordinates, device/category or chronological row, CSV cohorts, payload, missing counts, nearest-rank percentiles and histogram boundaries. All13 cases pass; output `outputs/graph-summary-validation/timeline-oracle.json`. `work/compare_timeline_upas.py` executes actual uPAS Timeline/Gantt data preparation AST on a common3-request fixture:6 endpoints and3 lifetimes match. Remaining differences (relative ns-derived ms, reindexed rows, current-session/device lanes, block layer) are recorded in `timeline-upas-comparison.json` and the contract. Actual native figures reviewed: timeline-all, gantt-all and gantt-missing; no mockups.

An intermittent initial Gantt gesture failure was traced to the QA harness: presets apply after the first frame, leaving old LBA screen coordinates while the background Timeline summary is loading. Preset application now clears stale geometry, point gestures require a point inside the actual plot, and the harness freezes a stabilized click target. A regression verifies geometry invalidation. Final point/area/zoom reruns pass; this was a verification-input defect, not a fabricated product timing failure. A separate initial all-target test attempt collided with the still-running GUI EXE on Windows; rerun after GUI exit passed.

Validation: desktop lib84 passed,2 environment-dependent ignored; all Windows-compatible desktop/protocol/types all-target suites passed. Clippy with-D warnings, cargo fmt and git diff checks passed. The100K selection regression remains below5s. The included Timeline UI follows the existing include-file formatting convention.

Pending full Goal work: remaining43-feature/acquisition audit (including completion CPU/hardware queue/I/O wait feasibility), complete all-graph real/synthetic/missing/large matrix, equivalent-input comparisons for remaining rows, baseline/new loading/filter/render/memory performance, current original source/install manifest recheck, compatible Release/agent/object packaging, recoverable installation and actual installed launch. Current phone lacks root/su; actual root eBPF probe acceptance remains unverified, while Perfetto capture→stop→save→reload was already verified. No overall completion or installation claim is made here.

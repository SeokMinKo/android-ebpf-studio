# 2026-09-07 window and filter checkpoint — Goal remains active

This is verified progress, not full uPAS parity or a Release installation.

- Added six Explore presets (indices12..17): fixed-window BW, IOPS, cumulative payload, device Busy%, active ms, idle ms. Existing request axes0..16 and presets0..11 retain their indices. One statistical sample per elapsed window drives the linked Summary histogram/percentiles/CDF/CSV; values are never repeated per I/O. Empty windows count, final partial rates use actual duration. Effective width increases when needed to cap20,000 windows and is shown.
- Internal boundaries belong to the following window; final analysis end is inclusive. Click/rectangle selects whole drawn windows and their assigned request keys. BW range snaps to the first/last selected window edge. Cumulative values stay anchored to analysis start. Tests cover empty-window selection, unavailable activity, boundary-point uniqueness, partial duration, non-R/W payload exclusion and multi-device unions. Empty selected windows can be zoomed when bounds exist.
- Actual latest trace device8:0 has8247 I/O and8 windows. Six native presets passed independent raw-observation reconstruction of every window/direction/count/byte/active duration/rate/percentile/histogram/CSV. Native area selects4 windows/607 I/O; point selects1 window/43 I/O. Direct detail CSV row counts and R/W bytes match. Evidence: window-12..17, window-window-area, window-point, window-oracle.json; runner work/verify_windows.py.
- Found and fixed a real PID Clear bug: egui DragValue's delayed lost-focus commit restored its previous buffered number after query reset. Clear now rotates numeric input identities. Headless real mouse/text regression fails with the fix removed and passes with it restored. Evidence pid-before-fix.log and pid-regression-proof.json; script work/prove_pid_regression.py always restores source.
- Native PID7780 ->2147483647 ->Clear yields2477 ->0 ->8294 requests across filtered engine, Process lanes, Summary and table I/O CSV. First two states keep device8:0 Busy532653188ns and interval7412882341ns. Clear also removes the device filter, correctly widening all-device Busy to537906882ns. Native input and CSV verification: work/verify_pid.py, pid-input, pid-oracle.json.
- Native Next visited all35 pages/139 distinct device+comm+PID labels and Previous returned to page34; raw observations independently matched all groups, Summary stayed8247 I/O. Evidence lane-pages and lane-pagination-oracle.json; work/verify_lane_pages.py. Initial8s harness timeout was too short; bounded45s scenario budget succeeds in20.24s. Oracle was corrected to use explicit unavailable PID and UTF-8, rather than assume numeric0 or Windows-default decoding.
- Added background table and graph-cohort I/O CSV buttons. Each request is one row; candidate paths/confidence share a JSON array; CompletedIo JSON retains source evidence. Optional timing/identity fields stay blank. Explicit ns/512Bsector/exclusive-end/extent/RWpayload columns. Regression covers selected-only rows, multi-origin confidence, quoted Korean paths, unavailable queue timing, empty export. Existing original-session export remains separate; source-path overwrite is rejected.
- Added Summary Host BW Busy/Idle share pie and exact CSV percent, with the same wall-time/union/coverage gates. Actual run: Busy532.653188ms (7.1855%), Idle6880.229153ms (92.8145%); reconstructed Perfetto estimate. window-idle-share.png was visually inspected and CSV independently checked.
- Fixed right-edge clipping for large LBA Histogram/CDF/Violin by sharing bounded plot/tick helpers. Headless renderer checks actual tick glyph bounds inside280px/340px panels. QA reports the correct preset and distinguishes windows from contributing I/O. Point QA now chooses visible points or scrolls its plot into view; a changed desktop viewport had placed the former first target below the visible scroll area. Window oracle now requires qa_timed_out=false, selected=true and exact expected window count.
- Latest compatible all-target run passed (desktop lib74 passed/2 environment-dependent ignored; reanalysis7 passed; other protocol/types/desktop integrations passed). Latest affected-lib rerun74/2 passed after Busy/Idle share. Clippy with -D warnings, fmt and diff checks pass. Evidence window-final-tests.log, window-final-lib-tests.log, window-final-clippy.log. Scripts/evidence are under the task's work/ and outputs/graph-summary-validation/ respectively.

## Still required by the full objective

Rolling C2C/D2D BW; continuous Busy/Idle interval-duration trends and distributions; idle-separated cumulative bursts; Timeline/Gantt/connected footprint; hardware queue/IOWait acquisition audit and feasible extensions; remaining43-feature correspondence and equivalent-input comparisons; complete real/empty/missing/large graph and filter matrix; window Apply/Clear edge cases, particularly disjoint Y-selected windows and their range envelope; like-for-like loading/filtering/rendering/memory performance; current source/installed-manifest recheck; compatible Release build, recoverable backup/install and actual installed launch. Current window presets are Explore-only; existing Compare request presets remain available. Current phone lacks su, so actual eBPF probe acceptance remains distinct from verified native Perfetto capture.


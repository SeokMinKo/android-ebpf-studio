# Connected Footprint continuation — Goal still active

2026-09-07. Previous turn made verified burst progress committed as3b45b8e. This continuation adds connected request lifetimes to the existing LBA Footprint and preserves the linked right Summary. No installation or overall completion is claimed.

- New Explore preset23 and connection checkbox. Horizontal measured issue-to-completion segment, hollow issue marker, filled completion marker and vertical LBA extent. Missing issue retains its completion point without inventing a lifetime. Device lanes stay separate; FilePath/Process/file-process candidate grouping and all-group pagination are reused.
- Click either endpoint or the segment to select one request. Rectangles retain completion-endpoint semantics so Summary, filters, Host BW and CSV stay consistent. Unit changes retain connection state. Fixed operation coloring is explicit; unrelated scatter controls are hidden in connected view.
- Source uPAS5306-5366 uses the same send/complete horizontal segment. Independent original-event oracle matched every full-resolution point's issue/completion coordinates, shared origin,512B-sector extent, direction and identity; candidate memberships remain distinct from unique global counts. LBA percentiles/histogram and graph I/O CSV match the selected raw cohort.
- Native real8247-I/O run passed: Combined1 device group, Process139 groups, Read filter7415, point1, area554. Real legacy184-I/O trace passed with12 FilePath groups. A three-request deterministic native fixture clicked a line midpoint over40px from its endpoints and selected exactly request1,400ms lifetime and4096 bytes. Initial fixture lacked mandatory sequence/schema fields and was rejected; corrected valid wire records and footer passed. That initial harness failure was not a product calculation failure.
- Regression covers same-LBA separate devices and unmeasured issue evidence, checking no fabricated start and unchanged LBA Summary population. Compatible all-target tests passed (desktop lib80 passed/2 environment-dependent ignored), followed by narrow-Summary glyph test after compact tick formatting. Compatible Clippy/fmt/diff checks pass.
- Native screenshots reviewed: connected-segment and connected-read. Replaced unsupported arrow glyph with plain text; Combined lane height330px and explicit numeric Y ticks make large LBA ranges readable. Whole-number compact ticks omit redundant decimals to reduce crowded Summary labels.

Evidence: outputs/graph-summary-validation/connected-oracle.json, connected-tests.log, connected-clippy.log and connected-*.json/png/summary.csv/io.csv. Helpers: work/verify_connected.py, work/connected_fixture.py, work/checkpoint_connected.py. Footprint report contains full-resolution geometry even when display stride is active; this does not claim every point was drawn simultaneously.

Remaining required scope: Timeline/Gantt and corresponding linked summaries; acquisition-field feasibility/implementation (completion CPU, hardware queue/IOWait where viable); full43-source corresponding-input/real/synthetic/missing/large matrix; all Apply/Clear/filter edge semantics; before/after performance/memory; compatible Linux/Android artifacts; original source/install manifest recheck; recoverable Release backup/install and actual installed launch. Current phone lacks su, limiting physical eBPF acceptance only; previously verified Perfetto capture/save/reload is separate evidence. Continue useful work with the full Goal active.

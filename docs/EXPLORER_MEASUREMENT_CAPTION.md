# Explorer display counts

The caption distinguishes requests selected by the renderer sampling limit from requests omitted because one of the chosen axes has no measurement. A smaller plotted count alone is not evidence of downsampling. Both reasons can be reported when present. A three-event physical Read fixture with no FS/UFS measurements reproduces the previous false sampled label.

The native table-keyboard gate waits for initial scale/layout stabilization before Enter. The first-frame widget ID changed at QA scale 1, so the earlier focus belonged to a different ID when Enter arrived. The gate independently checks the opened request key against the last paired block completion and preserves the source hash. This is a QA timing correction, not evidence of a broken settled user keyboard path.
